@model MVC.Models.ListGroupsModel

@{
    ViewData["Title"] = "Show Groups";
    var groups = Model.Groups;
}
<table>
    <tr>
        <th>Name</th>
        <th>Color</th>
        <th>Note</th>
    </tr>
    <tr>
        <td>
            <a href="/Delta/EditGroup?groupId=-1"><button type="button">Create new unit</button></a>
        </td>
    </tr>
    @foreach (var group in groups)
    {
        <tr>
            <td>@group.Name</td>
            <td>@group.Color</td>
            <td>@group.Note</td>
            <td><a href="/Delta/EditGroup?groupId=@group.Id"><button type="button">Show details</button></a></td>
        </tr>
    }
 </table>
